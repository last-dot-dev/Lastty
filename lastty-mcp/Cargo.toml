[package]
name = "lastty-mcp"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "lastty-mcp"
path = "src/main.rs"

[dependencies]
pane-protocol = { path = "../pane-protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
